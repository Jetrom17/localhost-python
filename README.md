# Instação mais intrução

```
git clone https://github.com/Jetrom17/localhost-python/ && clear && cd localhost-python && rm -r README.md && clear && ls && echo "Execute python3 live.py" && echo "Depois acesse seu navegador, coloque seu IP + PORT"
```
